events {}

http {
  upstream agent_backend {
    server agent_blue:8080;
  }

  server {
    listen 80;

    location / {
      proxy_pass http://agent_backend;
      proxy_http_version 1.1;
      proxy_set_header Connection "";
      proxy_set_header Host $host;
    }
  }
}