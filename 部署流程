前端：
pnpm build
scp -r dist ubuntu@1.13.182.xx:/home/ubuntu/agent-stack/
docker restart nginx   或者 docker compose up -d --force-recreate nginx

后端：
本地开发环境 mvn clean package
scp app.jar blue/
docker restart agent_blue